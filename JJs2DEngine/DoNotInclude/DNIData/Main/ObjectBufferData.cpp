#include "MainDNIpch.h"
#include "ObjectBufferData.h"

namespace JJs2DEngine
{
	ObjectBufferData::ObjectBufferData() : texCoords(0.0f), texSize(1.0f), pos(0.0f), size(1.0f), texLayer(0), texIndex(0)
	{
	}

	ObjectBufferData::~ObjectBufferData()
	{
	}

}